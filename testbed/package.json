{
    "type": "module",
    "scripts": {
        "start": "python3 -m http.server 8000",
        "test": "python3 -m http.server 8000 & node test/test.js && kill %1"
    },
    "devDependencies": {
        "fs-extra-promise": "^1.0.1",
        "playwright": "^1.25.1"
    }
}
